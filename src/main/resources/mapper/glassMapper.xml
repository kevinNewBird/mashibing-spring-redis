<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashibing.springboot.mybatis.dao.GlassMapper">
    <select id="findAll" resultType="java.util.Map">
        select * from glass
    </select>
</mapper>